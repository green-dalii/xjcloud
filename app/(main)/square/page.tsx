'use client'

import SquareFeed from '@/components/square/SquareFeed'

export default function SquarePage() {
  return (
    <div style={{ background: 'var(--bg-ink)', minHeight: '100vh', paddingTop: 56 }}>
      <SquareFeed />
    </div>
  )
}
